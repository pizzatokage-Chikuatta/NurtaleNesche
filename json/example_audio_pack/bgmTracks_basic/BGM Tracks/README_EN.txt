Type: bgmTracks
Example file: bgm.example.training_room.json

What this data does:
It registers a BGM track id and the file used for that track.

Fields shown in the example:

- `id`: canonical BGM track id.
- `file`: BGM audio file path.
- `volumeMultiplier`: volume multiplier for this BGM track.