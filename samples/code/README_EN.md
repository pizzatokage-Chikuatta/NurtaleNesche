# Developer Reference Packs

These folders are not public JSON mod examples. They are reference material for maintainers who are working on a build that intentionally supports code-backed mod extension points.

Normal mod authors should start with `../Example Mod Packs` instead.