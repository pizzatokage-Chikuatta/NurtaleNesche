Type: interactionLoadouts
Example file: profile.example.training_crate.json

What this data does:
It groups interaction command ids into one interaction profile.

Fields shown in the example:

- `id`: canonical interaction profile id.
- `commands`: ordered command ids available in this profile. The example includes the built-in `interaction.not_now`, then the example open/leave commands from interaction option metadata.